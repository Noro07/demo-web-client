sudo: required
dist: trusty
language: node_js
node_js:
  - "8"
addons:
  apt:
    packages:
      - google-chrome-stable
install:
  - npm run link-packages
script:
  - npm run test
  - npm run test:e2e
cache:
  directories:
    - node_modules
